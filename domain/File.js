module.exports = class File {

    constructor (name, file, taskid, reviewid, projectid) {

        this.name = name
        this.file = file
        this.taskid = taskid
        this.reviewid = reviewid
        this.projectid = projectid


    }
}